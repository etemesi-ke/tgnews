/// Represents a document's instance
pub struct TDocument {
    pub url: String,
    pub site_name: String,
    pub published_time: i64,
    pub title: String,
    pub description: String,
    pub article: String,
}
